#include<map>
#include <unordered_map>
#include<string.h>
#include<bits/stdc++.h>
using namespace std;

int main(){
    int size;
    cin>>size;
    string a;
    cin>>a;
    int anton = 0;
    int danik = 0;
    for(int i=0; i<size; i++)
    {
        if(a[i]=='A')
            anton++;
        else
            danik++;
    }
    if(anton>danik)
        cout<<"Anton"<<endl;
    else if(anton<danik)
        cout<<"Danik"<<endl;
    else
    {
        cout<<"Friendship"<<endl;
    }
}
